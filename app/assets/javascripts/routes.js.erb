

(function() {
  var angular, root, thisApp;

  root = typeof global !== "undefined" && global !== null ? global : window;

  angular = root.angular;

var thisApp = angular.module('ImportClient', ['ngRoute', 'ngResource','Devise','ngCookies','satellizer']);



thisApp.config([
  '$routeProvider', '$locationProvider','AuthProvider','$authProvider', function($routeProvider, $locationProvider,AuthProvider,$authProvider) {
    
   
    $authProvider.facebook({
      clientId: '154002081631469'
    });

    $authProvider.google({
      clientId: '197590823023-dnfnrn6a9mvj0m8i2a77a03kokfv28eh.apps.googleusercontent.com'
    });

    $authProvider.github({
      clientId: 'c9555f872062fe0ea0c9'
    });

    $authProvider.linkedin({
      clientId: '75edu1ryoloj13'
    });


    $authProvider.oauth2({
      name: 'foursquare',
      url: '/auth/foursquare',
      clientId: 'MTCEJ3NGW2PNNB31WOSBFDSAD4MTHYVAZ1UKIULXZ2CVFC2K',
      redirectUri: window.location.origin || window.location.protocol + '//' + window.location.host,
      authorizationEndpoint: 'https://foursquare.com/oauth2/authenticate'
    });



    $routeProvider.when('/home',{
      templateUrl: "<%= asset_path('home/index.html') %>",
      controller: 'WelcomeHomeCtrl'
    });
    $routeProvider.when('/',{
      templateUrl: "<%= asset_path('welcome/index.html') %>",
      controller: 'WelcomeCtrl'
    });
    $routeProvider.when('/signup',{
      templateUrl: "<%= asset_path('welcome/signup.html') %>",
      controller: 'WelcomeCtrl'
    });
    $routeProvider.when('/reset/:token',{
      templateUrl: "<%= asset_path('welcome/reset.html') %>",
      controller: 'WelcomeCtrl'
    });
     $routeProvider.when('/resetpassword',{
      templateUrl: "<%= asset_path('welcome/resetpassword.html') %>",
      controller: 'WelcomeCtrl'
    }).otherwise({
      redirectTo: '/'
    });
  }
]);

  root.thisApp = thisApp;
  
}).call(this);




